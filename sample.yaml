sample:
  description: firmware-nordic-nrf91, the accelerometer and microphone Edge Impulse example for Zephyr
    application
  name: firmware-nordic-nrf91
common:
    tags: introduction
    harness: console
    harness_config:
      type: one_line
      regex:
        - "Hello World! (.*)"
